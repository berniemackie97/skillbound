import '../../styles/20-pages.characters.css';

type CharactersLayoutProps = Readonly<{
  children: React.ReactNode;
}>;

export default function CharactersLayout({ children }: CharactersLayoutProps) {
  return children;
}
