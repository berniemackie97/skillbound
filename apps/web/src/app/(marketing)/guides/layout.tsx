import '../../styles/20-pages.guides.css';

type GuidesLayoutProps = Readonly<{
  children: React.ReactNode;
}>;

export default function GuidesLayout({ children }: GuidesLayoutProps) {
  return children;
}
