import '../../styles/20-pages.guides.css';

type ProgressionLayoutProps = Readonly<{
  children: React.ReactNode;
}>;

export default function ProgressionLayout({
  children,
}: ProgressionLayoutProps) {
  return children;
}
